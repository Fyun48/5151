# Bash scripts must use LF line endings (CRLF breaks `set -o pipefail` on Linux).
*.sh text eol=lf
# YAML (CI workflows, compose) must use LF so regex/onBlock tests and Linux tooling agree.
*.yml text eol=lf
*.yaml text eol=lf
